# A8: Best lambda, temporal encoding disabled.
# Placeholder: lambda_contrastive set to 0.5 until best lambda is confirmed.
model:
  use_recon: true
  use_contrastive: true
  use_shared_private: false
  voxel_temporal_encoding: false   # ablate the learnable temporal bias

loss:
  use_reconstruction: true
  use_contrastive: true
  lambda_contrastive: 0.5          # UPDATE to best lambda from A3-A5
  use_shared_private: false
